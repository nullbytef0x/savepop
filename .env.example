# Public address that visitors' browsers use to reach the SavePop API.
# For a remote deployment, replace localhost with the server IP or API domain.
SAVEPOP_API_URL=http://localhost:5120/

# Public hostname used for generated sitemap URLs (without http:// or https://).
WEB_HOST=localhost:5173
